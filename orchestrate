#!/bin/bash

# Wrapper script for task orchestration (interactive mode)
# This delegates to the TypeScript implementation

# Pass all arguments to the TypeScript implementation
# The TypeScript file will set auto=false for interactive mode
exec bun run scripts/orchestrate-auto.ts "$@"