#!/bin/bash

# Wrapper script for autonomous task creation
# This delegates to the TypeScript implementation which uses the channelcoder SDK

# Pass all arguments to the TypeScript implementation
exec bun run scripts/task-creator-autonomous.ts "$@"